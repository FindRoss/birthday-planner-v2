import styled from 'styled-components';

export const MessagePill = styled.div`
  position: fixed;
  display: flex;
  align-items: center;
  padding: var(--size-400) var(--size-700);
  background: #ffffff;
  box-shadow: 0 1px 3px 0 rgb(0 0 0 / 10%);
  bottom: 1rem;
  left: 1rem;
  border-radius: var(--radius-md);
`;